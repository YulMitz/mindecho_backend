# Test environment - safe to commit, uses isolated test database only
NODE_ENV=test
DATABASE_URL=postgresql://mindecho_test:test_password_123@localhost:5555/mindecho_test

# Auth
JWT_SECRET=test-jwt-secret-key-for-integration
JWT_EXPIRE=24h

# LLM (fake key, integration tests should mock LLM calls)
GEMINI_API_KEY=fake-test-key
ANTHROPIC_API_KEY=
